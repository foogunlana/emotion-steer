[project]
name = "emotion-steer"
version = "0.1.0"
description = "Steer a small model's emotions live while it talks or writes a story."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "torch>=2.14.0",
    "transformers>=5.17.0",
    "uvicorn[standard]>=0.52.4",
]

[tool.uv]
package = false
